{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap 
metadata:
  name: stock-config
data:
    {{- range $key, $value := .Values.configMap.data }}
    {{ $key }}: {{ $value }}
    {{- end }}
{{- end }}