apiVersion: v1
kind: Service
metadata:
  name: grafana
spec:
  selector: 
    app: grafana
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.grafana.servicePort }}
      targetPort: {{ .Values.grafana.targetPort }}
  type: NodePort