apiVersion: v1
kind: Service
metadata:
  name: prometheus
spec:
  selector:
    app: prometheus
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.prometheus.servicePort }}
      targetPort: {{ .Values.prometheus.targetPort }}   
  type: NodePort