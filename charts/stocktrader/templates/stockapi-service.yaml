apiVersion: v1
kind: Service   
metadata:
  name: stockapi
spec:
  selector:
    app: stockapi
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.stockapi.servicePort }}
      targetPort: {{ .Values.stockapi.targetPort }}
  type: NodePort